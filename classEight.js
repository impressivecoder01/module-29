// keys, values, entries, delete, seal, freeze
const employee = {
    name: 'rana', 
    designation: 'dev',
    salary: 20000,
    experience: 1
}
const keys = Object.keys(employee)
console.log(keys)